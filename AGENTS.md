# Codex Credit Optimizer Skill

## 基本方針
- まず全体を読まず、ファイル名・ディレクトリ構造・git diff・検索結果から必要箇所を絞る。
- 巨大ファイル、生成物、画像、音声、動画、ビルド成果物、lockファイルは原則読まない。
- node_modules, dist, build, coverage, .next, .vite, out, public/assets, public/images, public/audio, public/video は必要がない限り探索しない。
- 1回の作業で編集するファイルは原則3〜5個までに抑える。
- 広範囲修正が必要な場合は、先に影響範囲を短く報告してから段階的に進める。
- 似た処理を探す場合は全文読解ではなく rg / git grep を優先する。
- エラー調査ではログ全文を読まず、最初の重要エラー、該当ファイル、該当行前後だけを見る。
- 修正後は必ずgit diffの要約を行い、変更理由を短く説明する。

## 同人ゲーム開発向けルール
- ゲームバランス、難易度、借金、AP、釣り、伐採、採掘、栽培、戦闘、クラフト、娘苗、仲間、イベントに関わる数値は勝手に大きく変更しない。
- 既存のゲーム仕様を変える場合は、変更前後を明確にする。
- UI文言、セリフ、キャラクター名、販売ページ向け表現は勝手に改変しない。
- 画像・音声・動画などのアセット名やパスは、実在確認してから変更する。
- public 配下の素材は、ビルド時 import ではなく実行時パスで扱う方針を優先する。
- 成人向け同人ゲームであっても、コード作業では年齢確認・注意文・販売導線・UI整理など安全な実装に限定する。
- 不要なリファクタリング、命名変更、フォーマット全面変更は避ける。
- 依頼された不具合・機能に直接関係する箇所のみ触る。

## 調査手順
1. まず `git status --short` を確認する。
2. 次に関連しそうなファイル名だけを検索する。
3. 必要なら `rg` で関数名・UI文言・エラー文を検索する。
4. ファイルを読む場合は該当行周辺から読む。
5. 修正前に、編集予定ファイルを短く列挙する。
6. 修正後に、変更ファイル・変更理由・確認結果を短く報告する。

## 禁止事項
- いきなりプロジェクト全体を読まない。
- 巨大ログをそのまま出力しない。
- package-lock.json / pnpm-lock.yaml / yarn.lock を不用意に読まない・編集しない。
- アセットフォルダを丸ごと走査しない。
- 仕様外の大規模リファクタリングをしない。
- 画像や音声素材を勝手に削除・リネームしない。
- 既存の動作を確認せずに推測だけで修正しない。
